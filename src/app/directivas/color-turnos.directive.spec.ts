import { ColorTurnosDirective } from './color-turnos.directive';

describe('ColorTurnosDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTurnosDirective();
    expect(directive).toBeTruthy();
  });
});
