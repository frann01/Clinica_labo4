import { ColorMiPerfilDirective } from './color-mi-perfil.directive';

describe('ColorMiPerfilDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorMiPerfilDirective();
    expect(directive).toBeTruthy();
  });
});
