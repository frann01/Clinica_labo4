import { SubTitulosDirective } from './sub-titulos.directive';

describe('SubTitulosDirective', () => {
  it('should create an instance', () => {
    const directive = new SubTitulosDirective();
    expect(directive).toBeTruthy();
  });
});
